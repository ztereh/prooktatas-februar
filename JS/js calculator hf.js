
    function plusz() {
    calc.eredmeny.value = calc.a.value*1 + calc.b.value*1;
    }
       
     function minusz() {
    calc.eredmeny.value = calc.a.value - calc.b.value;
    }
    function szorzas() { 
    calc.eredmeny.value = calc.a.value * calc.b.value;
    }
    function osztas() {
    calc.eredmeny.value = calc.a.value / calc.b.value;
    }
    